# Godot 4+ specific ignores
.godot/           # Editor cache/configuration (safe to ignore)
/android/         # Android export templates and build files
.import/          # Imported asset cache directory
*.import          # Metadata for each imported file (useless in version control)
export_presets.cfg   # Optional; stores export settings for builds
